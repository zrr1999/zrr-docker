version: "3"
services:
  dev-main:
    image: zrr1999/zrr-main:latest
    tty: true
    privileged: true
    pid: host
    container_name: dev-main
    ports:
      - 10000:22
    volumes:
      - '/var/run/docker.sock:/var/run/docker.sock'
      - '/mnt:/mnt'
      - '~/workspace:/workspace'
    command: /bin/bash -c "/run.sh && zsh"
    restart: unless-stopped

  dev-paddle:
    image: zrr1999/zrr-main:latest
    tty: true
    privileged: true
    pid: host
    container_name: dev-paddle
    ports:
      - 10000:22
    volumes:
      - '~/micromamba/envs/cuda118:/nvidia/cuda118'
      - '~/workspace:/workspace'
      - '/mnt:/mnt'
    command: /bin/bash -c "/run.sh && zsh"
    restart: unless-stopped
